/**
 * 認証フォーム（ログイン・新規登録）コンポーネントの引数の型定義
 */
type PropForAuthenticationForm = {
  apiUrl: string;
  errorMessage: string;
  description: string;
  buttonText1: string;
  buttonText2: string;
  navDestination: string;
};
